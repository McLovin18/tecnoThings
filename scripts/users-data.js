/**
 * 🚨 ERROR: users-data.js DEPRECADO Y ELIMINADO
 * 
 * Este archivo ha sido eliminado por razones de SEGURIDAD.
 * 
 * ❌ NO USE:
 * import { getCurrentUser } from './users-data.js';
 * 
 * ✅ USE FIREBASE EN SU LUGAR:
 * import { getCurrentUser } from './firebase-auth.js';
 * 
 * Si ves este error, necesitas actualizar el import en tu código.
 */

throw new Error(
  '🚨 FATAL: users-data.js ha sido eliminado. ' +
  'Todos los archivos deben usar firebase-auth.js en lugar de users-data.js. ' +
  'Revisa tus imports y actualiza a: import { getCurrentUser } from "./firebase-auth.js"'
);

